1.linux文件描述符fd和文件指针flip:
fd只是一个整数，在open时产生。起到一个索引的作用，进程通过PCB中的文件描述符表找到该fd所指向的文件指针filp
open()操作返回的是一个文件描述符fd,内核会在每个进程空间中维护一个文件描述符表，表中存储fd和flip对应，每个打开的文件都是通过此表中的文件描述符fd引用
fopen()操作返回的是一个FILE结构体指针，该结构体包括了文件描述符fd，FILE结构可以函数是对文件描述符fd直接操作的系统调用封装，它的优点是带有I/O缓存
每个进程在PCB中都保存了一个文件描述符表，文件描述符就是这个表的索引，每个文件描述符对应一个系统中打开的文件，已经打开的文件中系统中用
 file结构体表示，而文件描述表中的每一个文件描述符fd都指向一个file结构体

2.
